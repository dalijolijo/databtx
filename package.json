{
  "name": "datacash-btx",
  "version": "1.2.0",
  "description": "post data over bitcore",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "build": "browserify index.js --standalone datacash-btx > dist/datacash.js && uglifyjs dist/datacash.js > dist/datacash.min.js"
  },
  "keywords": [
    "btx",
    "bitcore"
  ],
  "author": "unwriter",
  "license": "ISC",
  "unpkg": "dist/datacash.min.js",
  "dependencies": {
    "bitcore-explorers": "^1.0.1",
    "bitcore-lib-btx": "git://github.com/dalijolijo/bitcore-lib-btx#master"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "mocha": "^5.1.1",
    "uglify-es": "^3.3.9"
  }
}
